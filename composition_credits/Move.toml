[package]
name = "composition_credits"
edition = "2024"
license = "Apache 2.0"
authors = ["Miso Labs, Inc. (build@miso.network)"]

[dependencies]
miso = { local = "../../protocol" }
partyos = { local = "../../party", rename-from = "miso_party" }
miso_credit = { local = "../../credit" }
