[package]
name = "release_credits"
edition = "2024"
license = "Apache 2.0"
authors = ["Miso Labs, Inc. (build@miso.network)"]

[dependencies]
miso = { local = "../../miso-protocol" }
partyos = { local = "../../miso-party", rename-from = "miso_party" }
miso_credit = { local = "../../miso-credit" }
