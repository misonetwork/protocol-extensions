[package]
name = "release_genre"
edition = "2024"
license = "Apache 2.0"
authors = ["Miso Labs, Inc. (build@miso.network)"]

[dependencies]
genre = { local = "../../genre" }
miso = { local = "../../protocol" }
per_track = { local = "../../per-track" }
